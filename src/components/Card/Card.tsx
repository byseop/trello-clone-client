import styled from 'styled-components';
import CardFeature from './CardFeature';

const Card = styled(CardFeature)`
  background: #fff;
  padding: 6px 8px;
  border-radius: 3px;
  box-shadow: 2px 2px 4px rgba(0, 0, 0, 0.04);
`;

export default Card;
